{
  "name": "sentiment-analysis-platform",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "dev": "tsc && node dist/server.js",
    "build": "tsc",
    "start": "node dist/server.js",
    "railway:build": "rm -rf dist && npm run build",
    "railway:start": "npm run start",
    "preview": "npm run build && npm run start",
    "clean-port": "fuser -k 3000/tcp 2>/dev/null || true",
    "test": "curl http://localhost:3000",
    "postinstall": "tsc"
  },
  "dependencies": {
    "@hono/node-server": "^1.12.0",
    "cors": "^2.8.5",
    "hono": "^4.9.8",
    "sqlite3": "^5.1.6"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
